version: '3'

services:
  npm_termgame:
    build: .
    volumes:
      - ./app/src:/app/src
      - ./app/webpack.config.js:/app/webpack.config.js
      - ./dist:/app/dist
    command: >
      sh -c "npm run serve"
    ports:
      - "9000:9000"
